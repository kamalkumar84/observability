services:
  loki:
    image: grafana/loki:3.5.3
    container_name: loki
    restart: unless-stopped

    ports:
      - "3100:3100"

    command:
      - -config.file=/etc/loki/config.yaml

    volumes:
      - /data/loki/config/config.yaml:/etc/loki/config.yaml
      - /data/loki/data:/loki
